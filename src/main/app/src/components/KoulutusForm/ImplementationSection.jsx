import React from 'react';

import Typography from '../Typography';

const ImplementationSection = props => {
  return (
    <div {...props}>
      <Typography>
        Tähän koulutukseen ei ole vielä liitetty toteutuksia
      </Typography>
    </div>
  );
};

export default ImplementationSection;
