import React from 'react';

import ValintakoeFields from '../ValintakoeFields';

export const ValintakoeSection = ({ name, language }) => {
  return <ValintakoeFields name={name} language={language} />;
};

export default ValintakoeSection;
