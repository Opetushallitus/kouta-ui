export { default } from './ValintaperusteForm';

export const validate = values => {
  const errors = {};

  return errors;
};

export const initialValues = {
  kieliversiot: {
    languages: ['fi', 'sv'],
  },
};
