export { default } from './ValintaperusteetForm';

export const validate = values => {
  const errors = {};

  return errors;
};

export const initialValues = {
  kieliversiot: {
    languages: ['fi', 'sv'],
  },
};
